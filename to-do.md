# To Do
[ ] Remove the "online" icon
[ ] Remove the "hint" that explains how to show the code solution.
[ ] Change the width of the questions and steps.
[ ] Allow to use tags like <div> when writing steps.
[ ] Add a "Material" / "theory" section where the material can be presented.
[ ] Add a "pause" button for the editor so that OneCompiler doesn't have to load every time a change is made on the webpage. - Maybe this is what the "online" button should do. Click it to change it to "offline" and the compiler doesn't load. Instead there is a message saying "Compiler Offline". But this is only for the lab page builder. This option is not available to the created pages. The compiler there is always online.
